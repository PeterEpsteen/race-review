const bikeType = ['Mountain', 'Road', 'Cyclocross', 'Track', 'Fixed'];
const raceType = ['XC', 'Enduro', 'Road Race', 'Criterium', 'Circuit Race', 'Downhill', 'Track'];

const RaceParams = {
    bikeType: bikeType,
    raceType: raceType,
};

module.exports = RaceParams;